FINDINGS: critical=2 important=4 minor=3

critical:
  - id: c1
    task: "Task 2: git.lua — add get_staged_stat()"
    step: "Step 1 (failing test) — stub sets vim.v.shell_error inside vim.fn.system"
    concern: "vim.v is a Neovim read-only metatable proxy; assigning vim.v.shell_error = 1 inside a stub is not guaranteed to work."
    recommendation: "Refactor to read shell_error into a local after vim.fn.system as existing functions do; in tests stub vim.v as a regular table in before_each: vim.v = setmetatable({}, {__newindex = rawset})."

  - id: c2
    task: "Task 7: input/rich.lua — concurrency scheduler"
    step: "Step 1 — assumes vim.schedule fires synchronously"
    concern: "No vim.schedule stub exists in the test suite; scheduler tests will hang or fail."
    recommendation: "Add vim.schedule stub in before_each: vim.schedule = function(fn) fn() end; restore after each test."

important:
  - id: i1
    task: "Task 8: input/rich.lua — full prepare() pipeline"
    step: "Step 3 — prepare() calls require('aicommits.ui.picker').show_status"
    concern: "rich.lua (pure data-pipeline module) reaching into UI layer creates an unstubbed dependency in tests."
    recommendation: "Either stub picker in Task 8 integration tests, or move show_status calls out of rich.lua into commit.lua which owns UI concerns."

  - id: i2
    task: "Task 4: provider:summarize() — vertex.lua"
    step: "Step 3 — references local generate_token"
    concern: "Plan assumes generate_token is module-scope; if it is closure-scoped the new summarize() method cannot see it."
    recommendation: "Read vertex.lua first; if closure-scoped add an explicit hoist sub-step."

  - id: i3
    task: "Task 8: input/rich.lua — full prepare() pipeline"
    step: "Step 3 — done_tasks counter increments in nested sched.run callbacks"
    concern: "Complex double-call risk between per-chunk early-return paths and rollup path."
    recommendation: "Add a task_completed boolean per large-file task; gate done_tasks increment on it."

  - id: i4
    task: "Task 10: commit.lua — wire input.prepare"
    step: "Step 3 — pre-existing vim.defer_fn picker.show_status('The AI is analyzing...')"
    concern: "The deferred 500ms status will fire after input.prepare has set its own status, causing UI conflict."
    recommendation: "Remove or update the vim.defer_fn status call as part of Task 10 Step 3."

minor:
  - id: m1
    task: "Task 6: input/rich.lua — parsing helpers"
    step: "Step 3 — split_diff_by_file binary detection"
    concern: "is_binary uses file_diff:match('^Binary files') but first line is the 'diff --git' header — anchor fails."
    recommendation: "Drop the ^ anchor or scan individual lines."

  - id: m2
    task: "Task 11: full test suite green-check"
    step: "Step 1 — verification-only, skipping TDD failing test"
    concern: "Breaks heading/step contract."
    recommendation: "Acceptable as final smoke-check; document explicit TDD-rhythm exemption."

  - id: m3
    task: "Plan Overview"
    step: "Plan structure"
    concern: "Overview says 'nine independently completable tasks' but plan has 11."
    recommendation: "Update Overview to eleven."

LOOP_STATUS: NEEDS_FIX
